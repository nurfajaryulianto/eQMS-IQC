# Node / build
node_modules/
assets/css/tailwind.css

# Tailwind binary (tidak perlu di-commit)
tailwindcss.exe
tailwindcss

# Tailwind v3 config (tidak digunakan di v4, config ada di tailwind-input.css)
tailwind.config.js

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Editor
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# Logs
*.log
npm-debug.log*

# Environment (jangan pernah commit secret ke repo)
.env
.env.local
.env.*.local
